import {icon} from './icons.js'; import {money} from '../data/products.js'; import {state} from '../utils/store.js';
export function image(src,alt,klass='',eager=false){return `<div class="image-wrap ${klass}"><div class="skeleton"></div><img src="${src}" alt="${alt}" loading="${eager?'eager':'lazy'}" ${eager?'fetchpriority="high"':''} onload="this.parentElement.classList.add('loaded')" onerror="this.parentElement.classList.add('failed');this.remove()"><div class="image-fallback"><span>morrow</span><small>Image coming soon</small></div></div>`}
export function productCard(p){const liked=state.wishlist.includes(p.id);return `<article class="product-card" data-type="${p.type}"><a href="#/product/${p.id}" class="product-image">${image(p.image,p.name)}${p.badge?`<span class="badge">${p.badge}</span>`:''}</a><button class="heart ${liked?'active':''}" data-wish="${p.id}" aria-label="Save ${p.name}">${icon('heart')}</button><a href="#/product/${p.id}" class="product-info"><div><h3>${p.name}</h3><p>${p.type} · ${p.color}</p></div><strong>${money(p.price)}</strong></a></article>`}
export function productSkeletons(count=4){return Array.from({length:count},()=>`<div class="product-skeleton"><div class="skeleton"></div><i></i><i></i></div>`).join('')}
