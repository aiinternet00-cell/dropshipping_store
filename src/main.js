import {products,money} from './data/products.js'; import {state,save,cartCount} from './utils/store.js'; import {icon} from './components/icons.js'; import {image,productCard} from './components/ui.js';
const app=document.querySelector('#app'); let activeFilter='All';
const shell=content=>`<header><a class="logo" href="#/">MORROW</a><nav><a href="#/shop">Shop</a><a href="#/about">Our story</a></nav><div class="actions"><button data-search aria-label="Search">Search</button><button data-open-cart aria-label="Open cart">Bag <span data-count>${cartCount()}</span></button><button class="menu" data-menu aria-label="Open menu"><i></i><i></i></button></div></header><main>${content}</main><footer><div><a class="logo" href="#/">MORROW</a><p>Objects for animal lives.</p></div><div><h4>Explore</h4><a href="#/shop">Shop all</a><a href="#/about">Our story</a><a href="mailto:hello@morrow.pet">Contact</a></div><div><h4>Stay close</h4><p>Notes on good design and better days together.</p><form data-newsletter><input type="email" required placeholder="Email address"><button>${icon('arrow')}</button></form></div><small>© 2026 Morrow Goods</small></footer><div class="veil" data-close></div><aside class="drawer" data-drawer></aside><div class="toast" role="status"></div>`;
function home(){return shell(`<section class="hero">${image(products[0].image,'A dog at ease','hero-media')}<div class="hero-copy"><p class="eyebrow">Considered goods for companions</p><h1>Made for the<br>life you share.</h1><a class="button light" href="#/shop">Explore the collection ${icon('arrow')}</a></div></section><section class="intro"><p class="eyebrow">The Morrow philosophy</p><h2>They make a home feel alive.<br>We make objects worthy of it.</h2><p>Quietly beautiful essentials, thoughtfully made for the rituals of life with animals.</p></section><section class="featured"><div class="section-head"><div><p class="eyebrow">Daily companions</p><h2>Favorites, by design</h2></div><a href="#/shop">Shop all ${icon('arrow')}</a></div><div class="product-grid">${products.slice(0,4).map(productCard).join('')}</div></section><section class="manifesto"><div>${image(products[3].image,'Dogs outdoors')}</div><div><p class="eyebrow">Live well, together</p><h2>Less clutter.<br>More companionship.</h2><p>We begin with a real need, strip away the unnecessary, and choose materials that feel better with time.</p><a href="#/about" class="text-link">Read our story ${icon('arrow')}</a></div></section>`)}
function shop(){return shell(`<section class="page-head"><p class="eyebrow">The collection</p><h1>Useful things,<br>beautifully considered.</h1></section><section class="shop"><div class="filters">${['All','Walk','Rest','Dine','Travel','Play'].map(x=>`<button class="${x===activeFilter?'active':''}" data-filter="${x}">${x}</button>`).join('')}</div><div class="product-grid" data-grid>${products.filter(p=>activeFilter==='All'||p.type===activeFilter).map(productCard).join('')}</div><div class="no-results"><h2>No matches found</h2><p>Try another search.</p></div></section>`)}
function product(id){const p=products.find(x=>x.id===id);if(!p)return notFound();return shell(`<section class="detail"><div class="detail-image">${image(p.image,p.name)}</div><div class="detail-copy"><p class="eyebrow">${p.type} · ${p.color}</p><h1>${p.name}</h1><p class="price">${money(p.price)}</p><p class="lede">${p.description}</p><div class="choice"><span>Color</span><strong>${p.color}</strong></div><button class="button dark add" data-add="${p.id}">Add to bag <span>${money(p.price)}</span></button><button class="save ${state.wishlist.includes(p.id)?'active':''}" data-wish="${p.id}">${icon('heart')} Save for later</button>${['Details & care','Materials','Shipping & returns'].map((x,i)=>`<div class="accordion"><button data-accordion>${x}<span>+</span></button><div><p>${i===0?'Designed for daily use. Wipe clean with a soft, damp cloth.':i===1?'Selected durable, pet-safe materials with considered finishing.':'Complimentary shipping over $100. Returns accepted within 30 days.'}</p></div></div>`).join('')}</div></section><section class="featured"><div class="section-head"><h2>You may also like</h2></div><div class="product-grid">${products.filter(x=>x.id!==p.id).slice(0,3).map(productCard).join('')}</div></section>`)}
function about(){return shell(`<section class="page-head story"><p class="eyebrow">Our story</p><h1>A shared life deserves thoughtful design.</h1><p>Morrow was born from a simple belief: objects made for pets can belong naturally in the homes we care about. We favor honest materials, restrained forms, and details that make daily rituals easier.</p></section><section class="wide-image">${image(products[2].image,'A walk together')}</section>`)}
function notFound(){return shell(`<section class="empty"><p class="eyebrow">404</p><h1>This trail ends here.</h1><a class="button dark" href="#/">Return home</a></section>`)}
function cart(){const d=document.querySelector('[data-drawer]');const items=state.cart.map(i=>{const p=products.find(x=>x.id===i.id);return `<div class="cart-item">${image(p.image,p.name)}<div><h3>${p.name}</h3><p>${money(p.price)}</p><div class="qty"><button data-qty="${p.id}" data-delta="-1">−</button><span>${i.quantity}</span><button data-qty="${p.id}" data-delta="1">+</button></div></div><button class="remove" data-remove="${p.id}">Remove</button></div>`}).join('');const total=state.cart.reduce((s,i)=>s+products.find(p=>p.id===i.id).price*i.quantity,0);d.innerHTML=`<div class="drawer-head"><h2>Your bag</h2><button data-close>${icon('close')}</button></div><div class="cart-body">${items||`<div class="empty small"><h2>Your cart is waiting<br>for a new favorite.</h2><a class="button dark" href="#/shop" data-close>Explore Products</a></div>`}</div>${items?`<div class="cart-foot"><div><span>Subtotal</span><strong>${money(total)}</strong></div><button class="button dark" data-checkout>Checkout</button><small>Taxes and shipping calculated at checkout.</small></div>`:''}`;openDrawer()}
function openDrawer(){document.body.classList.add('drawer-open')} function closeDrawer(){document.body.classList.remove('drawer-open')}
function render(){const path=location.hash.slice(1)||'/';app.innerHTML=path==='/'?home():path==='/shop'?shop():path==='/about'?about():path.startsWith('/product/')?product(path.split('/')[2]):notFound();window.scrollTo(0,0)}
function toast(text){const t=document.querySelector('.toast');t.textContent=text;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}
document.addEventListener('click',e=>{const b=e.target.closest('button,a');if(!b)return;if(b.matches('[data-open-cart]'))cart();if(b.matches('[data-close]'))closeDrawer();if(b.matches('[data-add]')){const found=state.cart.find(x=>x.id===b.dataset.add);found?found.quantity++:state.cart.push({id:b.dataset.add,quantity:1});save();document.querySelector('[data-count]').textContent=cartCount();b.textContent='Added to bag';toast('Added to your bag')};if(b.matches('[data-wish]')){const id=b.dataset.wish,i=state.wishlist.indexOf(id);i<0?state.wishlist.push(id):state.wishlist.splice(i,1);save();b.classList.toggle('active');toast(i<0?'Saved to your wishlist':'Removed from wishlist')};if(b.matches('[data-filter]')){activeFilter=b.dataset.filter;render()};if(b.matches('[data-accordion]'))b.parentElement.classList.toggle('open');if(b.matches('[data-qty]')){const i=state.cart.find(x=>x.id===b.dataset.qty);i.quantity+=Number(b.dataset.delta);if(i.quantity<1)state.cart.splice(state.cart.indexOf(i),1);save();cart()};if(b.matches('[data-remove]')){state.cart=state.cart.filter(x=>x.id!==b.dataset.remove);save();cart()};if(b.matches('[data-menu]')){const d=document.querySelector('[data-drawer]');d.innerHTML=`<div class="drawer-head"><h2>Menu</h2><button data-close>${icon('close')}</button></div><nav class="mobile-nav"><a href="#/shop" data-close>Shop</a><a href="#/about" data-close>Our story</a></nav>`;openDrawer()};if(b.matches('[data-search]')){const q=prompt('What are you looking for?');if(q!==null){activeFilter='All';location.hash='/shop';setTimeout(()=>{const hits=products.filter(p=>(p.name+p.type+p.description).toLowerCase().includes(q.toLowerCase()));document.querySelector('[data-grid]').innerHTML=hits.map(productCard).join('');document.querySelector('.no-results').classList.toggle('show',!hits.length)},0)}};if(b.matches('[data-checkout]'))toast('Secure checkout will open when Shopify is connected')});
window.addEventListener('hashchange',()=>{closeDrawer();render()}); render();
